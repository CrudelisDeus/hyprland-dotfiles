{
  "layer": "top",
  "position": "top",
  "modules-left": ["custom/launcher", "cpu", "memory", "disk#root"],
  "modules-center": ["hyprland/workspaces"],
  "modules-right": ["network", "clock"],

  "hyprland/workspaces": {
    "format": "{icon}",
    "on-click": "activate",
    "all-outputs": true,

    "persistent-workspaces": {"*": 6},
    "format-icons": {
      "default": "",
      "active": "",
      "urgent": "",
      "empty": ""
    }
  },

  "custom/launcher": {
    "type": "custom/text",
    "format": "󰌽",
    "tooltip": false,
    "on-click": "rofi -show drun"
  },
  
  "cpu": {
    "interval": 2,
    "format": "  {usage}%"
  },

  "memory": {
    "interval": 2,
    "format": "  {percentage}%"
  },

  "disk#root": {
    "interval": 30,
    "path": "/",
    "format": " {used}"
  },

  "network": {
    "interval": 2,
    "format-wifi": "  {ipaddr}",
    "format-ethernet": "  {ipaddr}",
    "format-disconnected": "󰖪  No network",
    "tooltip-format": "{ifname}\n↑ {bandwidthUpBytes}  ↓ {bandwidthDownBytes}",
    "on-click": "nm-connection-editor"
  },

  "clock": {
    "format": "{:%d %b %y  %I:%M %p}",
    "tooltip-format": "{:%A, %B %d, %Y — %I:%M:%S %p}"
  }

}
